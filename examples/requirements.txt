# Host-side pins for examples/.
# smeme_apply_sample.py needs only fastmcp.
# smeme_langgraph_withholding.py additionally needs langchain[mcp] and langchain-openai.
# (not Core appliance dependencies — install in a separate venv)
langchain[mcp]==1.4.0
fastmcp==4.0.5
langchain-openai==1.6.2
langgraph==1.2.11
